#include <stdio.h>
#include <math.h>
#include <ctype.h>

float monto_item(int categoria, int cantidad, float precioUnidad);

void mostrarIems(int categoria, int cantidad, float precioUnidad, float precioCantidad);

float totalMedioPago(int pago, int cuotas, float total);

void mostrarMenu();


int main(){
    int categoria, cantidad, pago, cuotas;
    float

    return 0;
}